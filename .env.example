# Copy to .env and fill in — .env is gitignored, never commit real keys here.

# restcountries.com API key (used by dataset-gen to fetch country metadata).
# Get one at https://restcountries.com/plans
RESTCOUNTRIES_KEY=
